#!/usr/bin/env python

from distutils.core import setup

setup(name='sforce',
      version='0.0.1',
      description='Old school api client',
      author='the internet',
      packages=['sforce'],
      install_requires=["requests"],
)

